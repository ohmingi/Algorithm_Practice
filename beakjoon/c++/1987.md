<h2 data-ke-size="size26">백준 1987번 알파벳 (C++)</h2>
<p data-ke-size="size16">이번에 풀어본 문제는 깊이 우선 탐색(DFS)과 백트래킹을 활용하는 <b>백준 1987번 알파벳</b> 문제입니다. 보드에 적힌 알파벳을 중복 없이 방문하면서 말이 지날 수 있는 최대 칸 수를 찾는 문제입니다.</p>
<p data-ke-size="size16">문제는 아래 링크에서 확인할 수 있습니다.<br /><a href="https://www.acmicpc.net/problem/1987" target="_blank" rel="noopener noreferrer">백준 1987번 문제 바로가기</a></p>
<h3 id="💻-코드" data-ke-size="size23">💻 코드</h3>
<pre class="arduino"><code>#include &lt;iostream&gt;
#include &lt;algorithm&gt;

using namespace std;

int r, c;
char ch[21][21];
int cnt = 1;
bool used[26]; // 알파벳 사용 여부 배열

int dx[4] = {0, 0, -1, 1};
int dy[4] = {1, -1, 0, 0};

bool check(int x, int y) {
&nbsp; &nbsp; return x &gt; 0 &amp;&amp; x &lt;= r &amp;&amp; y &gt; 0 &amp;&amp; y &lt;= c;
}

void dfs(int x, int y, int depth) {
&nbsp; &nbsp; cnt = max(cnt, depth);

&nbsp; &nbsp; for (int i = 0; i &lt; 4; i++) {
&nbsp; &nbsp; &nbsp; &nbsp; int newx = x + dx[i];
&nbsp; &nbsp; &nbsp; &nbsp; int newy = y + dy[i];
&nbsp; &nbsp; &nbsp; &nbsp;&nbsp;
&nbsp; &nbsp; &nbsp; &nbsp; if (check(newx, newy)) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; int idx = ch[newx][newy] - 'A';
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; if (!used[idx]) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; used[idx] = true;
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; dfs(newx, newy, depth + 1);
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; used[idx] = false;
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; }
}

int main() {
&nbsp; &nbsp; ios::sync_with_stdio(false);
&nbsp; &nbsp; cin.tie(nullptr);
&nbsp; &nbsp;&nbsp;
&nbsp; &nbsp; cin &gt;&gt; r &gt;&gt; c;
&nbsp; &nbsp; for (int i = 1; i &lt;= r; i++) {
&nbsp; &nbsp; &nbsp; &nbsp; string str;
&nbsp; &nbsp; &nbsp; &nbsp; cin &gt;&gt; str;
&nbsp; &nbsp; &nbsp; &nbsp; for (int t = 1; t &lt;= c; t++) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; ch[i][t] = str[t - 1];
&nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; }

&nbsp; &nbsp; used[ch[1][1] - 'A'] = true;
&nbsp; &nbsp; dfs(1, 1, 1);

&nbsp; &nbsp; cout &lt;&lt; cnt;
}
</code></pre>
<h3 id="🔍-로직-설명" data-ke-size="size23">🔍 로직 설명</h3>
<p data-ke-size="size16">백트랙킹 및 dfs 방식으로 탐색해서 푸는 문제이다&nbsp;<br />알파벳을 저장하는 배열을 만들어 탐색한 알파벳은 다시 탐색하지 못하도록 하였고, 백트랙킹 방식을 통해 갈 수 있는 모든 경우의 수를 탐색하여 가장 많이 갈 수 있던 때의 경우를 저장 후 출력한다.</p>
<h3 id="✍️-느낀-점" data-ke-size="size23">✍️ 느낀 점</h3>
<p data-ke-size="size16">흔한 백트랙킹 문제라고 할 수 있다. 알파벳을 아스키 코드를 통해 int형으로 변환해서 visied를 관리해주고 백트랙킹 방식을 통해 모든 경우의 수를 탐색할 수 있도록 하였다. 간편하게 한번 재귀를 돌때마다 가장 최대값인지 비교하는 코드를 통해 복잡한 생각을 안 할 수 있었다.</p></code></pre>