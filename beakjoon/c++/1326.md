<h2 data-ke-size="size26">백준 1326번 폴짝폴짝 (C++)</h2>
<p data-ke-size="size16">이번에 풀어본 문제는 너비 우선 탐색(BFS)을 활용하여 최단 경로를 찾는 <b>백준 1326번 폴짝폴짝</b> 문제입니다. 징검다리를 최소 몇 번 점프하여 목표 지점까지 도달할 수 있는지 계산하는 전형적인 최단 거리 문제였습니다.</p>
<p data-ke-size="size16">문제는 아래 링크에서 확인할 수 있습니다.<br /><a href="https://www.acmicpc.net/problem/1326" target="_blank" rel="noopener noreferrer">백준 1326번 문제 바로가기</a></p>
<h3 id="💻-코드" data-ke-size="size23">💻 코드</h3>
<pre class="cpp"><code>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;queue&gt;

using namespace std;

int main() {
&nbsp; &nbsp; ios::sync_with_stdio(false);
&nbsp; &nbsp; cin.tie(nullptr);

&nbsp; &nbsp; int N;
&nbsp; &nbsp; cin &gt;&gt; N;
&nbsp; &nbsp; vector&lt;int&gt; jump(N + 1);
&nbsp; &nbsp; for (int i = 1; i &lt;= N; i++) cin &gt;&gt; jump[i];

&nbsp; &nbsp; int start, goal;
&nbsp; &nbsp; cin &gt;&gt; start &gt;&gt; goal;

&nbsp; &nbsp; vector&lt;int&gt; dist(N + 1, -1);
&nbsp; &nbsp; queue&lt;int&gt; q;
&nbsp; &nbsp; dist[start] = 0;
&nbsp; &nbsp; q.push(start);

&nbsp; &nbsp; while (!q.empty()) {
&nbsp; &nbsp; &nbsp; &nbsp; int cur = q.front();
&nbsp; &nbsp; &nbsp; &nbsp; q.pop();

&nbsp; &nbsp; &nbsp; &nbsp; // 오른쪽으로 점프
&nbsp; &nbsp; &nbsp; &nbsp; for (int next = cur + jump[cur]; next &lt;= N; next += jump[cur]) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; if (dist[next] == -1) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; dist[next] = dist[cur] + 1;
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; q.push(next);
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; &nbsp; &nbsp; }

&nbsp; &nbsp; &nbsp; &nbsp; // 왼쪽으로 점프
&nbsp; &nbsp; &nbsp; &nbsp; for (int next = cur - jump[cur]; next &gt;= 1; next -= jump[cur]) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; if (dist[next] == -1) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; dist[next] = dist[cur] + 1;
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; q.push(next);
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; }

&nbsp; &nbsp; cout &lt;&lt; dist[goal] &lt;&lt; "\n";
&nbsp; &nbsp; return 0;
}
</code></pre>
<h3 id="🔍-로직-설명" data-ke-size="size23">🔍 로직 설명</h3>
<p data-ke-size="size16">이 문제는 처음 보았을때 최소거리라고 하길래 dp?인가에 대해 먼저 접근이 되어서 a부터 b까지 dp에 최소거리를 저장하려고 살짝 생각했는데 바로 안될거 같다고 느껴 다른 방법을 생각하기 시작했다. 이게 a가 b보다 클 수 있어서 앞으로 가는 경우의 수 뒤로<br />가는 경우의 수까지 모두 고려해야 했기에 머리가 좀 아팠다. 결국은 bfs로 푸는 문제였는데 처음 a에서 접근할 수 있는 모든수(얖뒤 모두 포함)에 접근해서 거리를 +1로 만들고 (=bfs최단거리 만드는 dist랑 방식 똑같음)그리고 큐에 넣고 반복하는 방식이다</p>
<p data-ke-size="size16">dist[b]의 값이 -1에서 바뀌지 않았으면 그건 접근할 수 없다는 뜻이고 접근한 수는 그 순간이 바로 최단거리로 확정나기에 다시 값이 바뀔경우도 없다.</p>
<h3 id="✍️-느낀-점" data-ke-size="size23">✍️ 느낀 점</h3>
<p data-ke-size="size16">최단이 나오면 보통 dp나 bfs를 떠올리게 되는 뇌구조가 중요한것 같다. dp로 답이 없다 느껴지면 바로 bfs로 회선하는게 좋은 방향성인듯... 시간 너무 오래쓸수도</p></code></pre>