## 백준 1541번 잃어버린 괄호 (C++)

이번에 풀어본 문제는 **백준 1541번 잃어버린 괄호**이다.

문제는 아래 링크에서 확인할 수 있다.  
[백준 1541 문제 바로가기](https://www.acmicpc.net/problem/1541)

### 💻 코드

```
#include <iostream>
#include <stack>
#include <cmath>

using namespace std;

int main() {
  string s;
  cin >> s;

  stack<char> st;

  for (int i = 0; i < s.length(); i++) {
      st.push(s[i]);
  }

  int result = 0;
  int total = 0;

  while (!st.empty()) {
    if (st.top() <= '9' && st.top() >= '0') {
      int cnt = 0;
      int t = 0;
      while (!st.empty() && (st.top() <= '9' && st.top() >= '0')) {
        int tp = st.top() - '0';
        t += tp * (pow(10,cnt));
        cnt++;
        st.pop();
      }
      total += t;
    }
    else if (st.top() == '+') {
      st.pop();
    }
    else {
      st.pop();
      result -= total;
      total = 0;
    }
  }

  if (total > 0) {
    result += total;
  }

  cout << result;
}
```

### 🔍 로직 설명

일단 처음 봤을때 스택 문제인가? 싶다가도 계속 보다보니 아닌거같기도 하고 살짝 갸우뚱 했던 문제였다.

가장 작은 수를 구해야 했기에 그렇다면 뻬기에 중점을 줘야겠다 라고 생각한 후, 스택에 앞에서부터 담은 다음 숫자들을 -가 나올때까지 모두 더한 다음 -가 나오면 이때까지 더한 값들을 -처리해서 result에 담아서 게산하면 되겠다고 생각했다.

처음에는 숫자들이 하나의 문자타입으로 되어있으니 이걸 다 숫자로 처리한다음에 stack넣으려고 했는데, 이러면 +,-같은 문자를 담을 수없어서 일단 다 char타입 스택에 다 담고, 문자로 된 숫자가 나오면 이걸 정수로 처리한뒤 total이라는 변수에 다 더하고 문자로 된 숫자가 끝나면 이걸 result에 더한다

+가 나올경우는 어차피 계속 더하고 있었기에 그냥 pop을 해줬고, -일때는 이때까지 더한 total값을 -처리 시켜서 result에 더한다.   
그 다음 total을 다시 0으로 초기화해서 다음 total값을 구한다.

이때 중요한건 다 계산한뒤 무조건 수가 하나 남게 되는데 이는 양수이기에 마지막 result에 더해주면 최솟값이 나온다.

### ✍️ 느낀 점

처음에 제곱을 10^n 이런식으로 했는데 생각해보니 이건 xor연산이었다. ㅋㅋㅋㅋ... 

헷갈리지 말고 pow함수를 사용하자