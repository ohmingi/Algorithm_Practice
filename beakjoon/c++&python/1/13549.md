## 백준 13549번 숨바꼭질 3 (C++)

이번에 풀어본 문제는 **백준 13549번 숨바꼭질 3**이다.

문제는 아래 링크에서 확인할 수 있다.  
[https://www.acmicpc.net/problem/13549](https://www.acmicpc.net/problem/13549)

### 💻 코드

```
#include <iostream>
#include <vector>
#include <queue>

int dist[100001];
using namespace std;

void djk(int x, int y) {
  fill(dist,dist+100001,1e9);

  priority_queue<pair<int,int>, vector<pair<int,int>>, greater<pair<int,int>> pq;
  pq.push(make_pair(0,x));
  dist[x] = 0;

  while (!pq.empty()) {
    pair<int,int> curr = pq.top();
    pq.pop();

    if (curr.second == y) {break;}

    if (dist[curr.second] < curr.first) {continue;}

    if (curr.second - 1 >= 0) {
      if (dist[curr.second - 1] > curr.first + 1) {
        dist[curr.second - 1] = curr.first + 1;
        pq.push(make_pair(dist[curr.second - 1],curr.second - 1));
      }
    }
    if (curr.second + 1 <= 100000) {
      if (dist[curr.second + 1] > curr.first + 1) {
        dist[curr.second + 1] = curr.first + 1;
        pq.push(make_pair(dist[curr.second + 1],curr.second + 1));
      }
    }
    if (curr.second * 2 <= 100000) {
      if (dist[curr.second * 2] > curr.first) {
        dist[curr.second * 2] = curr.first;
        pq.push(make_pair(dist[curr.second * 2],curr.second * 2));
      }
    }
  }
}

int main() {
  int n,k;
  cin >> n >> k;

  djk(n,k);

  cout << dist[k];
}
```

### 🔍 로직 설명

이 문제 또한 어렵지 않은 다익스트라 문제이다. 문제에서 앞으로 뒤로, 그리고 2배로 이동가능하다 했을때 dfs를 쓸까했는데 너무 당연하게 시간 초과가 뜰것같아서 다익스트라로 접근했다.

기본문제에선 연결된 간선을 for문 돌려서 했지만 이 문제는 모든 정점들이 3개의 간선을 가지고 있다고 생각하면된다

+1,-1,\*2

+-1은 가중치를 1이라고 두고 두배는 가중치를 0이라고 두고 다익스트라를 돌리면 답이 나오게 된다.

우선수위큐가 자연스럽게 empty가 되어 끝나게 두기에는 너무 비효율적이라 찾고자하는 수에 접근하면 break를 사용하여 다익스트라 함수를 끝냈다.

### ✍️ 느낀 점

이 정도 문제까진 다익스트라를 쓸 생각만 한다면 크게 어렵지 않은 문제

다익스트라 함수의 형태를 잘 생각하고 놓치는 코드가 없는지 잘 생각해야겠다