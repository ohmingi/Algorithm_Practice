## 백준 1149번 RGB거리 (C++)

이번에 풀어본 문제는 **백준 1149번 RGB거리**이다.

문제는 아래 링크에서 확인할 수 있다.  
[백준 1149 문제 바로가기](https://www.acmicpc.net/problem/1149)

### 💻 코드

```
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int n;
int Min = 1e9;
vector<int> v[3];

void dfs(int x, int cnt, int ex) {
  if (x == n + 1) {
    if (Min > cnt) Min = cnt;
    return;
  }

  for (int i = 0; i < 3; i++) {
    if (i != ex) {
      cnt += v[i][x];
      dfs(x+1, cnt, i);
      cnt -= v[i][x];
    }
  }
}

int main() {
  cin >> n;

  int dp[1001][3];

  for (int i = 1; i <= n; i++) {
    int r,g,b;
    cin >> r >> g >> b;
    dp[i][0] = r;
    dp[i][1] = g;
    dp[i][2] = b;
  }

  for (int i = 2; i <= n; i++) {
    for (int t = 0; t < 3; t++) {
      if (t == 0) {
        dp[i][t] = min(dp[i-1][1], dp[i-1][2]) + dp[i][t];
      }
      if (t == 1) {
        dp[i][t] = min(dp[i-1][0], dp[i-1][2]) + dp[i][t];
      }
      if (t == 2) {
        dp[i][t] = min(dp[i-1][0], dp[i-1][1]) + dp[i][t];
      }
    }
  }

  //dfs(1,0,-1);

  //cout << Min << endl;

  int result = min(min(dp[n][0], dp[n][1]), dp[n][2]);
  cout << result;
}
```

### 🔍 로직 설명

처음에는 백트랙킹 방식으로 접근해서 풀었다. 시간복잡도가 초과할거같긴했는데 오랜만에 백트랙킹 써보면 좋을거같아서 감 잡을겸 써서 풀었다. 이전에 선택한 RGB값을 제외하고 dfs를 돌리면서 dfs가끝나면 다시 총값을 빼주고 남은 다른 RGB를 선택하면서 최솟값을 찾았다.

당연히 시관초과가 떴다. 시간복잡도가 O(2^n)이니까 0.5초를 가뿐히 넘는다.

그래서 당연히 dp문제겠구나 싶어서 접근했는데, 1차원dp로 풀려고 하니까 계속 틀린 제출이 되었다. (이것도 백트랙킹이랑 비슷하게 집에 도착할때마다 모든 경우의수 중 최고의 수만을 가지고 하려니까 이상해진듯)

결론은 이중 dp를 통해서 각각 집에서 각각 rgb를 선택했을때 가장 작은 값들을 각각 저장한뒤 마지막에 3개의 rgb중 가장 작은 값을 출력하면 O(n)만에 풀 수 있다.

### ✍️ 느낀 점

dp 문제 풀때 변수가 2개일때(이문제에선 집, rgb값) 이중 dp를 잘 사용해보자 / 백트랙킹은 시간복잡도가 많이 큰 알고리즘