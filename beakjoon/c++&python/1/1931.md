<h2 data-ke-size="size26">백준 1931번 회의실 배정 (C++)</h2>
<p data-ke-size="size16">이번 문제는 <b>그리디 알고리즘</b>의 대표적인 유형인 <b>백준 1931번 회의실 배정</b>입니다. 🗓️ 한 개의 회의실을 사용하려는 여러 회의에 대해, 겹치지 않게 가장 많은 수의 회의를 배정하는 방법을 찾는 문제입니다.</p>
<hr data-ke-style="style1" />
<p data-ke-size="size16">문제는 아래 링크에서 확인할 수 있습니다.<br /><a href="https://www.acmicpc.net/problem/1931" target="_blank" rel="noopener noreferrer">백준 1931번 문제 바로가기</a></p>
<hr data-ke-style="style1" />
<h3 id="💻-코드" data-ke-size="size23">💻 코드</h3>
<pre class="arduino"><code>#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;algorithm&gt;

using namespace std;

int main(){
&nbsp; &nbsp; ios_base::sync_with_stdio(false);
&nbsp; &nbsp; cin.tie(NULL);

&nbsp; &nbsp; vector&lt;pair&lt;int,int&gt;&gt; v;
&nbsp; &nbsp;
&nbsp; &nbsp; int n;
&nbsp; &nbsp; cin &gt;&gt; n;

&nbsp; &nbsp; for(int i = 0; i &lt; n; i++){
&nbsp; &nbsp; &nbsp; &nbsp; int a,b;
&nbsp; &nbsp; &nbsp; &nbsp; cin &gt;&gt; a &gt;&gt; b;
&nbsp; &nbsp; &nbsp; &nbsp; v.push_back({a,b});
&nbsp; &nbsp; }
&nbsp; &nbsp;
&nbsp; &nbsp; sort(v.begin(), v.end(), [](const pair&lt;int,int&gt; &amp;a, const pair&lt;int,int&gt; &amp;b){
&nbsp; &nbsp; &nbsp; &nbsp; if (a.second == b.second) {
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; return a.first &lt; b.first;
&nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; &nbsp; &nbsp; return a.second &lt; b.second;
&nbsp; &nbsp; });

&nbsp; &nbsp; int f = 0;
&nbsp; &nbsp; int cnt = 0;

&nbsp; &nbsp; for(int i = 0; i &lt; n; i++){
&nbsp; &nbsp; &nbsp; &nbsp; if(v[i].first &gt;= f){
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; f = v[i].second;
&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; cnt++;
&nbsp; &nbsp; &nbsp; &nbsp; }
&nbsp; &nbsp; }

&nbsp; &nbsp; cout &lt;&gt; cnt;
}
</code></pre>
<hr data-ke-style="style1" />
<h3 id="🔍-로직-설명" data-ke-size="size23">🔍 로직 설명</h3>
<p data-ke-size="size16">처음에 봤을때&nbsp; 일단 정렬을 해서 하는게 맞는거같은데 어떤식으로 정렬하지 + 처음값을 끝나는시간이 가장 작은 수를 선택해서 탐색을 돌려야 겠다까진 생각이 이어졌는데, 그 다음 어떤 값을 지정해야 할까라는 고민에서 좀 어려웠다....</p>
<p data-ke-size="size16">내가 한 생각은 음 그 다음 가장빠르게 시작하는 시간을 골라도 늦게끝나면 안되잖아 라는 고민이었는데,&nbsp; 끝나는시간을 기준으로 정렬을 하면 내가 선택한 회의의 끝나는 시간보다 크거나 같은 시간을 순서대로 탐색하면 그때 처음 만난수가 어차피 끝나는시간이 가장 빠른 수를 고른것이기에 복잡하게 고민할 필요가 없었다.</p>
<p data-ke-size="size16">정렬 알고리즘을 sort로 썼는데, second값을 기준으로 정렬을 해야 했기에 비교연산 추가해서 정렬했고, 끝나는시간이 같을때는&nbsp; 시작시간이 더 빠른 수를 선택할 수 있도록 했다.</p>
<p data-ke-size="size16">이렇게 하면 sort과정의 시간복잡도는 O(nlogn)의 시간이 걸리고, 백터에 입력받는 n, 탐색하는 n해서 다 더하면<br />O(nlogn)이 된다.</p>
<hr data-ke-style="style1" />
<h3 id="✍️-느낀-점" data-ke-size="size23">✍️ 느낀 점</h3>
<p data-ke-size="size16">그리디 풀면서 느끼는거지만 여기 부분에서 약점이 꽤 있다는것을 느꼈다. 뭔가 수학적 사고회로가 잘 안돌아간다...&nbsp;<br />더 많이 풀어야 겠다</p></code></pre>