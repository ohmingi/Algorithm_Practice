## 백준 1753번 최단경로 (C++)

이번에 풀어본 문제는 **백준 1753번 최단경로**이다.

문제는 아래 링크에서 확인할 수 있다.  
[https://www.acmicpc.net/problem/1753](https://www.acmicpc.net/problem/1753)

### 💻 코드

```
#include  <iostream>
#include <vector>
#include <queue>

using namespace std;

int V,E;
vector<pair<int,int>> adj[20001];
int dist[20001];
priority_queue<pair<int,int>, vector<pair<int,int>>, greater<pair<int,int>>> pq;

void dijkstra(int s) {
  fill(dist,dist+20001,1e9);

  dist[s]=0;
  pq.push({0,s});

  while (!pq.empty()) {
    int d=pq.top().first;
    int curr=pq.top().second;
    pq.pop();

    if (dist[curr] < d) {continue;}

    for (auto a : adj[curr]) {
      int next = a.first;
      int cost = a.second;

      if (dist[curr] + cost < dist[next]) {
        dist[next] = dist[curr] + cost;
        pq.push({dist[next],next});
      }
    }

  }
}

int main() {
  int k;
  cin >> V >> E >> k;

  for (int i = 1; i <= E; i++) {
    int u, v, w;
    cin >> u >> v >> w;

    adj[u].push_back(make_pair(v,w));
  }

  dijkstra(k);

  for (int i = 1; i <= V; i++) {
    if (dist[i] == 1e9) {
      cout << "INF\n";
    }
    else {
      cout << dist[i] << "\n";
    }
  }
}
```

### 🔍 로직 설명

기본적인 다익스트라 입문 문제다  
다익스트라에 대해 간단히 설명하면 가중치가 있을때 어느 점에서 어느점까지의 최단경로를 구할 수 있는 알고리즘이다

우선순위큐에 시작점에 대한 정보를 넣고 그 시작점에서 이어진 노드들의 값을 최신화 한다  
다음 이어진 노드들까지 현재의 최단거리 정보들을 큐에 넣는다  
다음 큐에서 접근하는 값은 가장 최단거리를 가지는 노드에 접근하기에 한번 큐에서 빠진 노드는 그 값이 최단거리가 되기에 중복 접근을 뺄 수 있다.

이렇게 최신화를 반복하다 보면 각각의 최단거리를 저장하는 배열을 구할 수 있게 된다

시간복잡도는 우선순위큐 삽입/ 삭제 시  logN의 복잡도를 가지는데 이때 n은 노드의 크기에 비레하므로 logE 이고 E <= V^이기에 logV라고 볼 수 있더. 최악으 경우로 모든 간선에 접근해서 탐색한다면 n(ElogV)를 가지게 된다

fill 함수는 단순 모든 노드에 접근해서 값을 초기화 함으로 n(V)을 가지게 된다

따라서 최종 시간복잡도는 n(ElogV)가 된다

### ✍️ 느낀 점

처음 다익스트라를 공부할땐 왜 큐를 통해 노드에 접근할시 그때 최단거리가 확정되는것에 제대로 이해를 못했었는데, 우선순위큐를 통해 접근을 하기에 다른 노드를 통해 짧은거리가 존재하려면 어쨌든 더 큰 값의 거리를 통해 접근할 수 밖에 없기에 최단거리가 확정이 된다.