## 백준 5972번 택배 배송 (C++)

이번에 풀어본 문제는 **백준 5972번 택배 배송**이다.

문제는 아래 링크에서 확인할 수 있다.  
[https://www.acmicpc.net/problem/5972](https://www.acmicpc.net/problem/5972)

### 💻 코드

```
#include <iostream>
#include <queue>
#include <vector>

using namespace std;

int n,m;
vector<pair<int,int>> v[50001];
int dist[50001];
priority_queue<pair<int,int>, vector<pair<int,int>>, greater<pair<int,int>>> pq;

void djk(int k) {
  fill(dist,dist+50001,1e9);

  dist[k] = 0;
  pq.push(make_pair(0,k));

  while (!pq.empty()) {
    pair<int,int> curr = pq.top();
    pq.pop();

    if (dist[curr.second] < curr.first) {
      continue;
    }

    for (auto a : v[curr.second]) {
      if (dist[a.first] > dist[curr.second] + a.second) {
        dist[a.first] = dist[curr.second] + a.second;
        pq.push(make_pair(dist[a.first],a.first));
      }
    }

  }
}

int main() {
  cin >> n >> m;

  for (int i = 0; i < m; i++) {
    int a,b,c;
    cin >> a >> b >> c;
    v[a].push_back(make_pair(b,c));
    v[b].push_back(make_pair(a,c));
  }

  djk(1);

  cout << dist[n] << endl;
}
```

### 🔍 로직 설명

단순한 다익스트라 문제이다. 다익스트라는 우선순위큐를 활용하여 방문즉시 어떠한 점에서 최단거리를 확정 짓는 알고리즘이다

우선순위큐에 거리가짧은것부터 접근을 해서 처리하기 때문에 그 점에 접근시 가장 짧은 거리로 접근하는 것과 같아진다

시간복잡도는 O(E log V)가 된다. 즉 간선마다 접근해서 우선순위큐에 넣고 빼는 것이다

이 문제에서는 O(m log n이 될것이다

한가지 다른 다익스트라 기본문제랑 살짝다른점은 단방향이 아닌 양방향인건데 다익스트라 로직 자체는 변함없다 

### ✍️ 느낀 점

다익스트라 사용하는 문제는 어떠한 점에서 어떠한 점까지 최단거리를 구할때

bfs랑 다른점은 bfs는 간선의 가중치가 없을때 사용하고 다익스트라는 가중치가 존재할때(양수)일때 사용한다