## 프로그래머스 42842번 카펫 (C++)

이번에 풀어본 문제는 **프로그래머스 42842번 카펫**이다.

문제는 아래 링크에서 확인할 수 있다.  
[https://school.programmers.co.kr/learn/courses/30/lessons/42842](https://school.programmers.co.kr/learn/courses/30/lessons/42842)

### 💻 코드

```
#include <string>
#include <vector>

using namespace std;

vector<int> solution(int brown, int yellow) {
    vector<int> answer;
    
    for(int i = 1; i <= yellow; i++){
        if(yellow % i == 0 && brown == (i + yellow / i)*2 + 4){
            answer.push_back(yellow / i + 2) ;
            answer.push_back(i + 2);
            break;
        }
    }
    
    return answer;
}
```

### 🔍 로직 설명

간단한 수학 문제이다.

처음에 문제를 해석할때는 되게 의문점이 많았다. 갈색은 항상 노란색을 둘러쌀수 있을정도로 주어지는거지 남는거지, 아니면 부족할 수 도 있는지 헷갈려서 되게 불친절한 문제군 라고 생각했지만 뭐 이런 문제가 많으니까 내가 잘 생각할 수 밖에없다.

문제 의도는 항상 노란색을 딱 둘러쌀 만큼만 갈색이 있다는 가정이다

그렇다면 갈색에 대해서는 일단 두고, 노란색에 대해서 직접 그림을 그려보면 여러가지 직사각형이 나온다

예를 들어 6개의 노란색 블록이 있다면 1\*6, 2\*3 두 가지의 직사각형이 나온다(6\*1, 3\*2는 각각 어차피 같은 테두리면적을 가지기에 신경쓰지 않아도 된다)

잘 보면 이는 약수인걸 알 수 있다 1,2,3,6 그도 그럴것이 가로\*세로가 면적이고 이것이 약수이기에 약수값 조합 자체가 가로와 세로의 길이가 된다.

결론은 약수일때 테두리 값이 갈색의 값이랑 일치하는지 비교하면서 약수를 탐색하면된다.

내 코드에서는 단순한게 n = yellow 탐색을 했지만 약수는 그 수의 제곱근까지만 탐색하면 되므로 총 루트n의 시간복잡도로 해결가능하다.

### ✍️ 느낀 점

수학문제는 문제에 말이 부족하지만 수학이기에 가장 정석적인 접근을 하는 방법을 공부를 해야할것 같다.

++ 약수 탐색할땐 제곱근까지만 탐색하는것을 버릇에 들이자