## 프로그래머스 42577번 전화번호 목록 (C++)

이번에 풀어본 문제는 **프로그래머스 42577번 전화번호 목록**이다.

문제는 아래 링크에서 확인할 수 있다.  
[https://school.programmers.co.kr/learn/courses/30/lessons/42577](https://school.programmers.co.kr/learn/courses/30/lessons/42577)

### 💻 코드

```
#include <string>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

bool solution(vector<string> phone_book) {
    bool answer = true;
    sort(phone_book.begin(), phone_book.end());
    
    for(int i = 0; i < phone_book.size() - 1; i++){
            if(phone_book[i] == phone_book[i + 1].substr(0,phone_book[i].length())){
                answer = false;
                break;
            }
    }
    return answer;
}
```

### 🔍 로직 설명

일단 문제를 보자마자 느낀건, 정렬을 하고 시작해야겠다고 생각했다. 정렬 후 고민을 해보았는데 처음에 내 생각은 정렬이 문자열은 문자열 크기대로 정렬이 된다라고 잘 못 생각하고 이중 포문을 사용해서 구하려고 했다

즉 각각의 문자열에 대해 그 뒤의 순서에 위치한게 더 문자열이 기니 뒤의 문자열들을 탐색후 substr을 사용해 찾으려고 했다 

당연히 n^2이 되어버려서 시간초과가 난다

그래서 고민해보니 정렬을 내가 잘 못 알고 있는것 같아서 찾아보니, 아스키 순서데로 정렬이 되고 문자열의 크기는 같은 문자열 시작부분을 가졌을때 고려되는 부분이었다.

예를 들어 "112", "1", "22" 이런식이라면

"1", "112", "22" 이런식으로 "1"과 "112"가 시작부분은 같은데 1이 더 짧은 문자열이니 1이 앞에 위치한다

그렇다면 접두어를 찾기위해서는 자기 자신의 바로 다음 문자열만 탐색하면 된다

그렇기에 sort시간복잡도인 nlogn만큼의 복잡도를 가지고, 시간초과도 해결할 수 있게 된다

### ✍️ 느낀 점

처음은 아니지만 제대로 프로그래머스로 알고리즘 준비하는게 처음이라서 이런 방식이 좀 낯설어서 어색했다.

ide에서는 다 자동완성해주고 함수도 다 알아서 채워주기에 편했는데, 이렇게 쌩으로 풀어보니 함수 사용법이 많이 약한것 같다

프로그래머스 형식으로 많이 풀어봐야 할 듯 하다

++ substr은 문자열 복제방식이라서 find함수를 사용하는 것이 더 효율적이다